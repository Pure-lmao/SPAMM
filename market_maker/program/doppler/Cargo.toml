[package]
name = "doppler"
description = "Vendored from https://github.com/blueshift-gg/doppler (sources unchanged)."
repository = "https://github.com/blueshift-gg/doppler"
license = "MIT"
edition = "2021"
version = "0.1.0"

[features]
default = []
std = []

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
   'cfg(target_os, values("solana"))',
] }
