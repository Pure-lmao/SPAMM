This directory is a verbatim vendor of the `doppler` crate from:

- Repository: https://github.com/blueshift-gg/doppler
- Path in upstream: `doppler/` (library crate)

The `Cargo.toml` at this root replaces upstream workspace inheritance so the crate builds standalone. All `src/*.rs` files match upstream `master` at vendor time.
